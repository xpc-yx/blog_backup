<% if (site.tags.length){ %>
	<div class="widget">
		<h4><%= __('tagcloud') %></h4>
		<ul class="tag_box inline list-unstyled">		
		<% site.tags.sort("length", -1).limit(80).each(function(item){ %>
			<li><a href="<%- config.root %><%- item.path %>"><%= item.name %><span><%= item.posts.length %></span></a></li>
		<% }); %>
		<% if (site.tags.length > 80) { %>
		   <li><a href="/tags">...<span><%= site.tags.length - 80 %></span></a></li>
		<% } %> 
		</ul>
	</div>
<% } %>
